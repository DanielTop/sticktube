# StikTube Project Memory

## Checkpoint #1 — 2026-01-03 03:45

### 📖 Эпизод (что произошло)
Работали над StikTube — клоном YouTube на Next.js 14. Сначала пользователь жаловался на баги с лайками и локализацию. Исправила совместимость админ-сессии с NextAuth — теперь лайки работают. Создала админ-панель `/studio/admin` для управления подписчиками (добавление/удаление). Потом пользователь попросил добавить Shorts — вертикальные видео как в TikTok. Добавила поле `isShort` в Prisma схему, создала страницу `/shorts` с вертикальным плеером и навигацией стрелками. Добавила 5 тестовых Shorts в seed. Но возникла проблема — Prisma клиент не видит поле `isShort`, нужно перегенерировать.

### 💡 Ключевые знания
- **О пользователе:** Ларри предпочитает русский интерфейс, не хочет Google OAuth (использует код 4444 для входа)
- **Технические:**
  - Проект: `/Users/larry/t/stiktube`
  - База: SQLite в `prisma/dev.db`
  - Админ вход: код `4444`, создаёт пользователя `admin@stiktube.local`
  - Порт: 3000
- **Концепции:** Shorts — вертикальные видео (9:16), прокрутка колесом мыши

### 🔧 Процедуры и методы
- ✅ **Что сработало:**
  - `npx prisma db push` для изменения схемы
  - `npx tsx prisma/seed.ts` для сидинга
  - Админ-сессия через куки `next-auth.session-token` с префиксом `admin-`
- ❌ **Что не сработало:**
  - После `db push` нужен `prisma generate` чтобы клиент увидел новые поля
  - Сервер нужно перезапустить после генерации

### 🎯 Текущее состояние
**Достигнуто:** 11 видео, 5 Shorts, админ-панель, локализация на русский
**Проблемы:** Prisma клиент не видит `isShort` — нужна перегенерация
**Следующие шаги:** Остановить сервер, перегенерировать Prisma, перезапустить

---

## Checkpoint #2 — 2026-01-07 02:15

### 📖 Эпизод (что произошло)
Сегодня был большой деплой! Ларри попросил найти stiktube на компе и залить на GitHub DanielTop/sticktube. Нашла папку в `/Users/larry/t/stiktube`, инициализировала git, создала репозиторий и запушила. Потом начались проблемы с деплоем на Railway — сначала не хватало checkbox компонента (добавила через shadcn), потом TypeScript ошибки с null в email полях (исправила типы в AdminPanel). Главная проблема была в том, что SQLite не работает в облаке — пришлось переходить на PostgreSQL. Ларри создал базу на Neon, я обновила schema.prisma на postgresql, запустила миграции и seed. Потом были проблемы с Next.js Image Optimization (отключила) и битые YouTube ID в shorts (заменила на реальные). Сайт заработал!

### 💡 Ключевые знания
- **О пользователе:** Ларри предпочитает захардкоженные секреты вместо env переменных ("захардкодь сам")
- **Технические:**
  - GitHub репо: `DanielTop/sticktube`
  - Railway URL: `sticktube-copy-production.up.railway.app`
  - Neon DB: `postgresql://neondb_owner:npg_WcfIo59LXmYp@ep-shy-hat-ad8xna5j-pooler.c-2.us-east-1.aws.neon.tech/neondb`
  - NEXTAUTH_SECRET захардкожен: `stiktube-super-secret-key-2026-prod`
- **Концепции:** На Railway/serverless SQLite не работает — нужен managed PostgreSQL

### 🔧 Процедуры и методы
- ✅ **Что сработало:**
  - `gh repo create DanielTop/sticktube --public --source=. --push`
  - `npx shadcn@latest add checkbox -y` для добавления компонентов
  - `prisma generate && next build` в build скрипте
  - `unoptimized: true` в next.config для Railway
- ❌ **Что не сработало:**
  - SQLite на Railway (нужен PostgreSQL)
  - @prisma/client в devDependencies (должен быть в dependencies)
  - Несуществующие YouTube ID для thumbnails

### 🎯 Текущее состояние
**Достигнуто:** Сайт задеплоен и работает, база на Neon, 11 видео + 5 shorts
**Проблемы:** Sidebar при загрузке выглядит криво (проблема гидратации)
**Следующие шаги:** Исправить гидратацию sidebar

### 🔗 Связи
Продолжение Checkpoint #1 — проект перешёл из локальной разработки в продакшен

---
